# Execution configs (`configs/execution`)

Execution simulator / cost model / slippage configs.

## Files
- `default.yaml`: baseline commission + slippage settings used by the execution simulator

## Notes
- Execution assumptions must be logged with every run (backtest and paper).
- These settings will later feed TCA calibration (implementation shortfall).
