# Paper trading configs (`configs/paper`)

Paper trading runner configs.

## Files
- `default.yaml`: paper broker mode and reconciliation settings

## Notes
- Paper-run configuration becomes the canonical entrypoint for Day 30 (go/no-go memo).
- `reconcile_on_start` controls whether the runner checks internal vs broker state on startup.
