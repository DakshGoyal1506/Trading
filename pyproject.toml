[project]
name = "Trading"
version = "0.1.0"
description = "Event-driven quant trading research/backtest/paper stack"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
