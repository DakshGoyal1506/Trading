# `src.utils`

Shared utilities used across modules.

## Contents
- `config.py`: YAML configuration loader (`load_yaml`)
- `paths.py`: canonical repository paths (`PROJECT_ROOT`, `CONFIGS_DIR`, `DATA_DIR`, `REPORTS_DIR`, `TESTS_DIR`)

These are Day 1 primitives and will be extended carefully as the stack grows.
